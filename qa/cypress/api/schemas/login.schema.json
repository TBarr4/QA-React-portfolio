{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Login Response",
  "description": "Schema for the auth login API response",
  "type": "object",
  "required": ["token"],
  "properties": {
    "token": {
      "type": "string",
      "minLength": 1,
      "description": "JWT authentication token"
    }
  },
  "additionalProperties": false
}
